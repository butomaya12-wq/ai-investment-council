POSTGRES_USER=aic
POSTGRES_PASSWORD=aic_dev_only
POSTGRES_DB=aic
DATABASE_URL=postgresql+psycopg://aic:aic_dev_only@localhost:5432/aic

# B3 runtime credentials/identity. Keep real values only in local environment or .env; never commit them.
OPENAI_API_KEY=
APCA_API_KEY_ID=
APCA_API_SECRET_KEY=
AIC_SEC_USER_AGENT=
