// Navigation only. This client code has no decision, risk, approval, or execution authority.
document.querySelector(".menu-button")?.addEventListener("click", () => {
  document.querySelector(".sidebar")?.classList.toggle("is-open");
});
